Flask web application that allows users to upload an image and automatically extract its dominant colors.

The app analyzes the uploaded image using colorgram, converts the colors to RGB and HEX, and displays:
The extracted color palette
Each color’s proportion in the image
The uploaded image itself